# Screenshots

Images referenced from the main [README](../../README.md). PNG, ~1280px wide.

- `buckets.png` — bucket list
- `objects.png` — object browser + preview pane
- `preview.png` — file preview (image/text/PDF)

Still missing, contributions welcome:

- Overview dashboard page
- Object browser in flat/paginated mode
- Bucket settings modal (public/private, policy, CORS, lifecycle)

File names are free as long as the main README is updated to match.
